/**
 * Careers content entry point.
 *
 * Prefer editing files under `./jobs/roles/` — see `./jobs/README.md`.
 * This module re-exports so existing imports keep working.
 */
export { jobs, allJobs, getJob, getAnyJob } from "./jobs";
export { sharedBenefits, defineJob } from "./jobs/shared";
